[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

[rpc_endpoints]
monad_testnet = "${NEXT_PUBLIC_MONAD_RPC_URL}"

# See more config options at https://book.getfoundry.sh/reference/config/
